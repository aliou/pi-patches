--- a/dist/core/extensions/loader.js
+++ b/dist/core/extensions/loader.js
@@ -408,7 +408,7 @@
             ? { virtualModules: await getVirtualModules(), tsconfigPaths: true }
             : { alias: getAliases() };
     const jiti = createJitiImpl(import.meta.url, {
-        moduleCache: false,
+        moduleCache: true,
         ...resolutionOptions,
     });
     const module = await jiti.import(extensionPath, { default: true });
--- a/dist/modes/interactive/interactive-mode.js
+++ b/dist/modes/interactive/interactive-mode.js
@@ -5088,6 +5088,10 @@
     // Command handlers
     // =========================================================================
     async handleReloadCommand() {
+        this.showWarning("/reload is disabled by the jiti-module-cache patch (moduleCache: true). Restart pi instead.");
+        return;
+    }
+    async handleReloadCommand_disabled() {
         if (this.session.isStreaming) {
             this.showWarning("Wait for the current response to finish before reloading.");
             return;
