{
  "name": "@aliou/pi-patches",
  "private": true,
  "type": "module",
  "pi": {
    "skills": [
      "./skills/creating-patches"
    ]
  },
  "scripts": {
    "patches:sync": "node scripts/sync-patches.mjs",
    "patches:check-offsets": "node scripts/check-patch-offsets.mjs",
    "test:patches": "node scripts/sync-patches.mjs --check && vitest run"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "0.85.0",
    "@earendil-works/pi-coding-agent": "0.85.0",
    "@earendil-works/pi-server": "0.85.0",
    "@earendil-works/pi-tui": "0.85.0"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
